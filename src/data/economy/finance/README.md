# Finance / economy reference corpus

Drop reference markdown articles here — this folder is meant for a large
corpus (hundreds to thousands of files), not curated personal notes. It's
searchable by default and can be rendered as 3D nodes from
**Settings → Finance Corpus in 3D**.

This folder ships empty on purpose — no sample/demo data.
