# Music galaxy

Drop your own audio files here (`.mp3`, `.wav`, `.ogg`, `.flac`, `.m4a`) —
each one becomes a node when the Music Galaxy feature is enabled from
Settings. This folder ships empty on purpose — no sample audio.
